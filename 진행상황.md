# 진행 상황 정리 (2026-08-22 갱신)

앱인토스 + 구글플레이 동시 출시 준비. **웹 호스팅 없이** 양쪽에 낼 수 있는 상태다.
등급분류(전체이용가 `SC-OM-260819-005`)를 받고, 아이콘·서명·AdMob·스토어 자산·제출 문서를
갖춰 **양쪽 스토어에 제출까지 마쳤다.** 실기기 검증도 끝냈다.

---

## 🔖 지금 어디까지 왔나

| | 상태 | 다음 액션 |
| --- | --- | --- |
| **구글플레이** | `versionCode 2` AAB 콘솔 업로드 완료(8/21). **`versionCode 3 / 1.2` AAB 빌드 완료(8/22**, 화폐 실사 그래픽·조작감 개선 포함, `android/app/build/outputs/bundle/release/app-release.aab`) — 콘솔 업로드는 사용자 몫 | 검토 결과 확인 → 비공개 테스트 12명 × 14일 |
| **앱인토스** | **라이브는 아직 `20260822-16`.** 번들 `-19`가 검수 중이라 인앱결제도 화폐 실사 그래픽도 **유저에게는 아직 안 나갔다** | 검수 승인 → **콘솔 웹 '출시하기'** (MCP 불가, 사용자가 눌러야 함) |
| **인앱결제** | 코드 연동 + sku 반영 + 환불 고지 완료. 콘솔 상품 `광고 제거` **ACTIVE**(사용자가 콘솔에서 전환, 8/22) — `ait.0000065259.795abd69.6e660bc08c.7373710053`, 3,500→3,850원 | 실기기에서 판매 화면 확인 |
| **번들 `20260822-19`** | **`REVIEWING`** (검수 요청 완료, 8/22). 인앱결제 + `-17`의 화폐 실사 그래픽·조작감·5회 상향까지 전부 포함. 실기기 결제 검증도 끝냄 | 결과 대기 → 승인 시 출시, 반려 시 `rejectMessages` 확인 |
| **등급분류** | 전체이용가 `SC-OM-260819-005`. 서류 3종 개정 후 **사용자가 게임위에 내용수정신고 접수 완료**(2026-08-22). 신고서 「변경내용」란 문안은 `등급분류/내용수정신고_변경내용.txt` | 결과 통보 대기 (7일 이내 재분류 대상 여부) |
| 코드 | `73e4461`까지 커밋·push 완료 (main). 인앱결제·환불 고지·등급분류 서류 개정 포함 | — |
| 환불 안내 페이지 | GitHub Pages 게시 완료 — <https://tngus842655.github.io/MoneyGame/refund.html> (HTTP 200 확인) | — |
| 판매자 정보 표시 | 구매 팝업·환불 안내 페이지(7항)·홈 링크 **세 군데 반영 완료**. 상호/대표자/사업자등록번호/이메일 표시. 영업소 주소는 자택이라 보류, 통신판매업 신고번호는 면제라 미표시 | **다음 번들에 실려야 반영됨** (검수 중인 `-19`엔 없음) |

### 다음 세션에서 바로 이어갈 것

0. **등급분류 내용수정신고 (사용자가 직접 신청 중)** — 유료 상품·프로모션 추가로
   제출 서류와 사실이 달라졌다. 서류 3종은 개정해 뒀다(`등급분류/README.md` 상단 참고).
   상품 판매 화면 캡처를 첨부해야 해서, 캡처 → 신청 순으로 진행 중.
1. **IAP는 연동·검증 끝.** sku 반영, 상품 `ACTIVE`, 실결제까지 성공(`ORDER_COMPLETED`).
   최소 지원 버전은 라이브 번들 `20260822-16`으로 등록돼 있어 **손댈 필요 없다** —
   새 번들은 그보다 최신이라 자동으로 조건을 만족한다. 실기기 검증은 **테스트 푸시**로
   할 것 (샌드박스에서는 결제가 안 된다).
2. **검수 결과 확인 → 출시** — `-19` 검수 요청 완료(`REVIEWING`). 승인돼도 **자동
   출시가 아니다** — 콘솔 웹 '앱 출시' 화면의 '출시하기'를 사용자가 눌러야 유저에게
   반영된다(MCP로 불가). 그전까지는 상품이 ACTIVE여도 라이브 번들에 구매 UI가 없어
   아무도 살 수 없다. **⚠️ 검수를 취소하면 그 번들은 재요청 영구 불가**다.
   반려되면 `bundle_list`의 `rejectMessages`를 보고 고쳐 새 번들로 다시 요청한다.
3. **구글플레이 검토 결과 확인** → 승인되면 비공개 테스트 트랙에서 테스터 모집
4. **정식 출시 승격 직전 AdMob 실제 ID 교체** → 새 AAB
   (토스 운영 ID는 8/21 적용 완료, AdMob은 아직 테스트 ID)

## ✅ 2026-08-22: 인앱결제 "광고 제거" 연동

무엇을 팔지부터 정했다 — **광고 제거(비소모품, 영구) 한 상품.** 기능 충전 팩·부활권은
보류. 첫 IAP라 구조 검증이 목적이고, 반응을 보고 늘린다.

**콘솔 등록값** — 상품 유형 비소모품 / 상품명 `광고 제거` / 설명
`배너 광고가 사라지고, 기능을 광고 없이 바로 사용해요`(30자, 한도 45) /
**공급가 3,500원 → 판매가 3,850원** (콘솔이 부가세 10%를 붙여 판매가를 계산한다.
유저가 내는 돈은 판매가) / 인앱결제 수수료 5%(VAT별도) /
아이콘 `store/iap/noads-1024.png`. 게임 버튼에 찍히는 가격은 하드코딩이 아니라
`getProductItemList`가 주는 `displayAmount`를 그대로 쓴다.

- **문서 MCP가 죽어 있었다** — `search_docs`가 "인앱결제"·"결제"는 물론 "광고" 같은
  확실한 단어에도 0건을 돌려줬다(인덱스 문제로 보임). 그래서 설치된
  `@apps-in-toss/web-framework`의 `dist/index.d.ts`와 `lib/apps-in-toss.min.js`
  구현부를 직접 읽어 규약을 확인했다. **다음에도 문서가 0건이면 이 방법이 빠르다.**
- **핵심 규약**(min.js에서 확인): `createOneTimePurchaseOrder`는 결제가 끝나면
  `processProductGrant({orderId})`를 부르고, 그 **반환값(boolean)을 SDK가 토스에 그대로
  통보**한다 → `completeProductGrant`를 따로 부를 필요 없음(그건 미완료 주문 복구용).
  `isSupported()`가 false면 **조용히 no-op 함수만 돌려주고 onError도 안 온다** — 직접 게이트해야 함.
  `getCompletedOrRefundedOrders`/`getPendingOrders`는 인자를 안 받는다(페이지네이션 불가).
  → **서버 없이 클라이언트만으로 완결된다.**
- `js/toss-iap.js` 신규 — 게임은 `window.NoAds`(`owned`/`sellable`/`product`/`purchase`/
  `onChange`)만 본다. 소유는 `localStorage`(`money-merge-noads`)에 두고 부팅·복귀 때
  주문 내역으로 보정(환불이면 회수). 조회 실패로 구매가 날아가지 않게, **못 찾았다고
  캐시를 지우지는 않는다.**
- **횟수 제한은 그대로 유지** — 구매해도 흔들기·동전제거 5회, 부활 1회다. 광고 시청만
  건너뛴다. 랭킹이 있는 게임이라 결제자만 무제한이면 순위표가 무너진다.
  파는 건 "광고 없음"이지 "더 셈"이 아니다.
- 배너도 없앤다(`body.no-ads`) — 토스 SDK에 배너 해제 API가 없어서 `toss-ads.js`가
  슬롯 내용을 비우고 `toss-banner` 클래스를 떼 레이아웃을 다시 계산한다. 부팅 시
  구매 상태면 배너 부착·보상형 프리로드를 아예 건너뛴다(그래서 `toss-iap.js`가
  `toss-ads.js`보다 **먼저** 로드돼야 한다).
- "📺 광고 보고 부활" → "💚 부활하기", HUD 버튼의 AD 배지도 CSS로 제거. 구매자에게
  광고 문구가 남아 있으면 거짓말이 된다.
- **주문서에서 그냥 나오면 버튼이 "결제 진행 중…"에 갇히는** 구멍이 있어,
  `visibilitychange`로 복귀할 때 주문 내역을 다시 확인하고 상태를 풀어 준다.
- 상품 아이콘 `tools/iap-icons.mjs` → `store/iap/noads-1024.png` (1024×1024).
  금지 링 + 빗금을 글자 **뒤로** 깔았다 — 위로 그으니 빗금의 흰 테두리(88px)가
  "AD"를 파먹어 두 겹으로 깨져 보였다.
- 로컬 검증(SDK 스텁 + localStorage): 상품 목록에서 비소모품만 선택 → 버튼에 가격 표시
  → 구매 → 지급 콜백 true → 캐시·배너·라벨·토스트 전부 반영 → 재부팅 후에도 유지 →
  게임에서 흔들기/동전제거가 광고 없이 즉시 실행되고 횟수는 5→4로 정상 차감.
- **구매는 기기가 아니라 토스 계정에 남는다.** 소셜 로그인·회원가입이 없어도 되는
  이유가 이것 — 토스 앱이 이미 사용자를 알고 있어서 `getCompletedOrRefundedOrders`가
  그 계정의 주문을 돌려준다. 그래서 기기를 바꿔도 같은 토스 계정이면 복원된다
  (localStorage는 캐시일 뿐). 검증: 캐시를 비운 상태에서 COMPLETED 주문만 주니
  소유가 살아났고, REFUNDED가 겹친 계정은 회수됐다.
- **복원이 끝나기 전에는 구매 버튼을 열지 않는다**(`booted` 플래그). 상품 조회 직후
  버튼을 띄우면, 기기를 바꾼 기구매자에게 "구매하기"가 잠깐 보여 이중 결제를 시도할
  수 있다. 조회 → 복원 → 미완료 주문 정리까지 끝난 뒤에 한 번에 연다.

### 구매 전 환불 고지 (2026-08-22)

유료 판매가 붙었으니 결제 전에 무엇을 사는지와 환불 조건을 알려야 한다. 세 군데에 넣었다.

- **`docs/refund.html` 신규** — 「구매 및 환불 안내」. 판매 상품·상품 내용·구매 내역의
  보관과 복원·청약철회(7일)·미성년자 결제·문의처. `privacy.html`과 같은 스타일.
  GitHub Pages로 나가므로 주소는 `.../MoneyGame/refund.html`. `docs/index.html`에도 링크 추가.
  판매자 정보(사업자등록번호 등)는 알 수 없어 **HTML 주석으로 틀만 만들어 뒀다** —
  필요하면 값 채우고 주석 해제.
- **구매 확인 팝업 `#buyDlg`** — 구매 버튼을 누르면 토스 주문서로 바로 넘기지 않고
  이 팝업을 먼저 띄운다. 유료 디지털 콘텐츠는 구매 전 고지가 필요하고, **"횟수가
  늘어나는 줄 알았다"는 오해로 인한 환불 분쟁도 여기서 막는다** — 경고 박스에
  "사용 횟수는 늘어나지 않아요"를 명시했다.
- **홈 하단 링크** — 개인정보처리방침 옆에 「구매 및 환불 안내」. 파는 환경(토스)에서만
  보인다. `js/legal-link.js`를 `a.legal` 전부 처리하도록 일반화해서 토스 인앱 브라우저로
  열린다(예전엔 `#linkPrivacy` 하나만 처리했다).

### 실기기 검증에서 나온 것 (2026-08-22, 실결제 3,850원)

사용자가 테스트 번들 `-18`에서 실제로 결제해 보고 확인한 내용.

- **연동은 끝까지 동작했다.** 콘솔 주문 상태가 `ORDER_COMPLETED`다 — 결제만 됐으면
  `PAYMENT_COMPLETED`에 멈춘다. `ORDER_COMPLETED`는 **지급 콜백 결과까지 토스에 정상
  통보됐다**는 뜻이라, 스텁으로만 보던 `processProductGrant` 경로가 실제로 검증됐다.
- 배너 사라짐 · AD 배지 사라짐 확인됨.
- **오탭 문제 (수정 완료, 번들 `-19`)** — 광고를 건너뛰면서 확인 팝업까지 같이 없앴더니
  HUD 버튼을 스치기만 해도 횟수가 날아갔다. 광고 흐름에서는 그 팝업이 **오탭 방어막을
  겸하고 있었다**는 걸 놓쳤다. 결제한 사람이 오히려 더 위험해진 셈. 이제 구매자에게도
  같은 팝업을 띄우되 문구에서 "광고를 보면"을 빼고 버튼은 「사용하기」로 바꾼다
  (`adFreeFlow` 플래그). 부활은 게임오버 화면의 버튼 자체가 확인 절차라 그대로 즉시 실행.
- 같이 정리: 기능 실행 분기가 광고·시뮬레이션광고·광고제거 세 군데에 복사돼 있던 것을
  `runFeature(kind, charged)` 하나로 합쳤다. 세 경로가 따로 놀 여지를 없앤다.
- 환불은 **게임 내 결제 내역 화면에서 이용자가 직접 신청**할 수 있다(사용자 발견).
- **⚠️ iOS 결제의 환불은 콘솔에 안 뜬다.** 애플로 결제된 건은 애플 환불 절차를 타기
  때문에 개발사가 콘솔에서 승인·거절할 수 없다 (콘솔 「환불 내역」 화면에도 "애플 환불
  건은 앱 마켓에서 진행돼요"라고 적혀 있다). 사용자가 신청했는데 `iap_refund_list`가
  계속 0건이라 한참 기다렸다 — **안 뜨는 게 정상이다.** `docs/refund.html`에 iOS는
  애플 절차를 따른다는 안내를 넣었다. 콘솔 환불 관리가 의미 있는 건 안드로이드 결제다.

### ⚠️ 등급분류 내용수정신고가 필요하다 (2026-08-22 확인)

사용자가 "약관 수정 같은 거 필요하지 않냐"고 물어 확인해 보니, **제출한 등급분류
서류가 사실과 달라진 상태였다.** 등급분류(`SC-OM-260819-005`, 전체이용가)를 받은 뒤
게임 내용이 수정되면 24시간 이내에 게임물관리위원회에 내용수정신고를 해야 하고,
위원회가 7일 이내에 등급 재분류 대상인지 통보한다.

서류와 달라진 것 네 가지 — **인앱결제 추가**(서류엔 "인앱결제 기능이 전혀 없습니다"),
**매일 5원 프로모션**(서류엔 "현금성 보상은 일절 없습니다"), 편의 기능 3회→5회,
화폐 그래픽을 단순 도형 → 실사 일러스트. 세 문서(`게임물개요`·`게임물내용설명`·
`게임실행방법`)를 모두 개정하고, 내용설명서에 **10항 「최초 등급분류 이후 변경 사항」**을
새로 넣었다. 개요는 1000자 한도라 977자로 맞췄다. `등급분류/README.md` 상단에
신고가 필요한 상태임을 적어 뒀다.

방어 논리는 "**파는 건 광고 없음이지 더 셈이 아니다**"로 잡았다 — 게임 내 재화 충전이
아니고, 확률형이 아니고, 사용 횟수 제한이 구매 여부와 무관해 난이도·랭킹에 유불리가
없다. 프로모션도 점수·승패와 무관한 확정 지급이라 우연성·베팅이 없다. 재분류까지
가지 않을 가능성이 높다고 보지만 **판단은 위원회 몫이다.**

나머지 검토 결과:
- **개인정보처리방침은 수정 불필요** — "결제·금융 정보 미수집"은 IAP를 붙여도 참이다
  (토스가 결제를 처리하고 게임은 주문번호·상품코드만 본다. 서버로도 안 보낸다).
- **환불 승인·거절은 개발사 몫**이다(MCP에 승인 tool이 없고 콘솔 웹 주문 관리에서만
  가능). 그런데 앱에 이용약관·환불 규정이 아예 없다 — 콘솔에서 동의한
  '인앱결제상품약관'이 환불 기준을 어디까지 정하는지 확인이 필요하다. (문서 MCP가
  죽어 있어 공식 가이드를 못 봤다.)
- **구글플레이는 지금 해당 없음** — Play 빌드에는 토스 결제 SDK가 동작하지 않아
  구매 경로가 없다. `PLAY_CONSOLE.md`의 '인앱 구매: 없음'은 그대로 맞다.

### 판매자 정보 표시 (2026-08-22, 진행 중)

전자상거래법 제10조는 **사이버몰 운영자**에게 상호·대표자·영업소 주소·사업자등록번호
표시를 요구한다. 인앱결제는 토스 플랫폼 안에서 일어나고 개발사는 입점 사업자라 의무
주체가 애매했는데(문서 MCP가 죽어 공식 가이드를 못 봤고 실결제 영수증에도 판매자
정보가 없었다), **개발사가 통신판매업자이고 미니앱 안에 직접 표시해야 한다**는 쪽으로
보고 진행한다. 근거 정황: 수수료 5%(스토어형 15~30%가 아닌 PG 수준), 콘솔의 거래처
등록 요구, 안드로이드 결제의 환불 승인·거절이 개발사 몫. 표시하는 쪽이 안 하는 쪽보다
안전하다는 판단이다. **확정된 공식 답변은 아니므로 토스 문의로 확인하면 좋다.**

넣을 곳 세 군데 전부 끝났다.
- ✅ **구매 확인 팝업** — 결제 직전 화면에 "판매자 엔에스에이치 · 문의 (이메일)" 한 줄.
- ✅ **`docs/refund.html` 7항 판매자 정보** — 상호 엔에스에이치 / 대표자 남수현 /
  사업자등록번호 374-38-01558 / 이메일. 기존 7항은 8항으로 밀었다.
- ✅ **홈 하단 「사업자 정보」 링크** — `refund.html#seller`로 연결. 「구매 및 환불 안내」와
  함께 파는 환경에서만 보인다(`sellable() || owned`).

- **영업소 주소는 사용자 결정으로 보류.** 사업장이 자택으로 등록돼 있어 공개를 원치
  않는다. 비상주 사무실(연 10~30만원대)로 옮길지 정한 뒤 채우기로 했다. 그때까지는
  주소 행을 넣지 않는다 — 불완전 표시 상태라는 걸 알고 내린 결정이다.
- **통신판매업 신고번호는 표시하지 않는다.** 사용자는 **일반과세자**라 간이과세자 면제
  조건은 못 쓰지만, "직전년도 통신판매 거래횟수 50회 미만"에 해당해(판매 시작 전이라
  0회) 현재는 신고 면제로 본다. **올해 50회를 넘기면 내년엔 신고 대상이 된다.**
- 이 건은 **출시를 막지 않는다** — 등급분류 신고·앱인토스 검수와 무관하다.

## ✅ 2026-08-22: 화폐 스프라이트를 실루엣 그대로 + 크기 위계 재조정

돈다발이 확대컷처럼 잘려 보인다는 피드백에서 시작해, 세 가지를 연달아 고쳤다.

- **돈다발(`bill-100000`) 잘림** — 오른쪽 인물·아래 겹이 잘렸다. `tools/money-assets.mjs`의
  변별 정밀 트림(0.80/15%)이 낱장 지폐용이라 3D 스택에선 네 변을 한계까지 깎은 게 원인.
  완만 트림(0.5/8%)으로 낮춰봤지만 "여전히 잘린다"는 피드백 → 최종적으로 **트림 전혀
  없이 원본 실루엣 그대로** (640×461, 비율 1.39)
- **물리 사각형 둘레의 "보이지 않는 충돌 공간"** — 실루엣을 살리니 bbox 모서리 투명부에
  조각이 공중부양(동전이 다발 위 허공에 얹힘). `tools/_analyze-stack.mjs`(신규, 실루엣 vs
  물리 갭 실측 도구)로 재보니 사각형+chamfer로는 다발의 깊은 모서리 빈 공간(최대
  37~110px)을 못 없앤다 → **물리를 실루엣 convex hull 12각형으로 교체**
  (`Bodies.fromVertices`). hull 좌표는 money-assets.mjs가 스프라이트에서 계산해 출력,
  game.js `STACK_HULL`에 복사(아트 바뀌면 재생성). hull/실루엣 면적비 1.055 —
  잘림 0 + 빈 공간 ~5%로 양쪽 다 해결. 잭팟(다발 두 개 충돌)도 hull끼리 정상 감지
- **낱장 지폐도 "잘린 직사각형" 느낌** — 정밀 트림 + 바탕 라운드 사각형 + 106% 스트레치
  조합이 원인. 지폐 4종은 **수평화 + 투명 여백 트림만** 해 물결 실루엣을 그대로 남기고
  (640×386, 비율 1.66), drawBill의 바탕 사각형을 없앤 뒤 1.08배 + chamfer h×0.18로 흡수
  (잔여 갭 ≤ ~4px). usd 벡터 지폐는 기존 방식 그대로
- **크기 위계 재조정** — 오만원↔돈다발, 500원↔천원이 거의 같아 보인다는 피드백.
  돈다발(195×140)을 기준으로 아래 티어를 전체적으로 ~8-10% 축소:
  동전 r 22/28/34/42 → 20/25/30/35, 지폐 100/121/143/167 → 90/109/130/154 (h는 새 비율).
  통(400px) 여유가 늘어 밸런스는 약간 순해짐
- **광고 기능 횟수 상향** — 통 흔들기·동전 제거 한 판 최대 사용을 각 3회 → **5회**
  (`MAX_FEAT`). 광고 노출 기회도 그만큼 늘어난다. README 동기화
- **동전도 실사 일러스트로 교체** — 사용자가 넣어준 한국 동전 시트
  (`brand/money/coin-raw.png`, 액면별 그림면·숫자면 8종)에서 money-assets.mjs가
  8종 전부를 자동 추출(축소본 알파 연결 성분으로 원 탐지 → 원형 마스크)해
  `public/money/coin-{10,50,100,500}[-back].png`(게임 지름 3배)로 저장. 물리 크기는
  그대로, drawCoin·홈 진화 체인이 스프라이트 우선 + 벡터 폴백(usd·로드 전)
- **동전 앞/뒷면 랜덤** — 조각마다 숫자면·그림면(다보탑/벼/이순신/학)이 반반 확률로
  나온다. 면은 `queueFaces`가 큐와 나란히 들고 가서 대기 미리보기와 떨어지는 면이
  항상 일치하고, 병합·비·자동진행처럼 미리보기 없는 스폰은 makePiece에서 랜덤.
  홈 진화 체인은 그림면으로 표시 (100원 병합·양면 혼재 검증 완료)
- **홈 화면 다듬기** — 타이틀 앞 💰 이모지 제거, 게임시작 버튼 확대
  (폰트 18→23px·폭 230→285px, 메뉴의 주 행동이라 다른 버튼보다 한 단계 크게),
  설명에 "왼쪽 위 🏆 랭킹에서 내 순위도 확인" 안내 추가
- **소스 정리** — 진입로 없는 달러 모드(`MODES.usd`, `best.usd`, usd CSS) 제거
  (되살리려면 git 히스토리 참고), 미참조 `brand/web-app-manifest-192x192.png`와
  스테일 `moneygame.ait` 삭제, `make-dist` 재실행으로 dist의 옛 스프라이트 정리.
  브랜드 512 아이콘은 android-assets.mjs의 원본이라 유지
- **랭킹 점수 상한 1억 → 10억** — 실플레이에서 1000만 원대가 나와 상위권이 1억에
  닿을 수 있다는 판단. `db/ranking_v2.sql` 3) 블록 수정, 사용자가 Supabase SQL
  Editor에서 실행해 서버 반영 완료(8/22)
- **관리자 추가: 용감한 펭귄407 (토스)** — `db/admin_stats.sql`의 admin_keys insert에
  `2c63a199-7699-4c60-8b07-006f2db8ddbf` 추가. Supabase 반영 완료(8/22, 사용자 실행)
- **홈 화면 관리자 버튼(📊)** — 랭킹 버튼 오른쪽, 관리자 기기에서만 보임.
  경량 `is_admin(uuid)` 함수를 admin_stats.sql 8)로 추가(집계·예외 없이 boolean만,
  관리자 ID 목록은 노출 안 됨), js/admin.js가 시작 시 한 번 물어 true면 hidden 해제.
  함수 미적용·실패 시엔 버튼이 숨겨진 채 타이틀 7연타가 백업 입구 (권한은 어차피
  get_admin_stats가 지킴). is_admin Supabase 반영 완료(8/22), 버튼 UI는 다음 번들에 실림
- **통계 화면 정렬 2건** — (1) 일간/주간/월간 캡션이 표 내용보다 6px 왼쪽으로
  삐져나가고 첫 캡션만 위 여백이 달랐다 → 캡션·안내문 좌우 여백을 표 셀 패딩(8px)에
  맞추고 first-child 예외 제거. (2) 표가 자동 레이아웃이라 머리글 길이 따라 표마다
  열 폭이 달라져 일간 카드만 숫자 열이 어긋나 보였다([126,102,102] vs [126,89,115]) →
  `table-layout:fixed`로 세 표 열 폭 통일 + 지난주/지난달 머리글을 시작일만 표기
  ("지난주 (8/10~)")로 줄여 고정 폭에서 줄바꿈 방지 (가짜 데이터 렌더로 픽셀 검증)
- **번들 `20260822-17` 검수 요청(8/22)** — 위 변경 전부(화폐 실사 그래픽·hull 물리·
  드롭 연타·광고 5회·홈 화면 정리·달러 모드 제거)를 담아 빌드 → MCP로 업로드·
  테스트 푸시·검수 요청까지 완료 (요청 #150847, 자동 스캔 통과 후 PENDING).
  → **이후 사용자가 검수 취소** (인앱 결제를 함께 넣으려고). 취소된 번들은 재검수
  요청이 영구 불가하므로 다음 변경은 새 번들로 요청해야 한다
- **드롭 연타 반응성** — 고정 550ms 쿨다운이 답답하다는 피드백. 최소 160ms(다음 조각
  교체 연출)만 남기고, 실질 게이트를 `spawnAreaClear()`로 교체: 직전 조각이 스폰
  지점에서 세로로 비켜났거나(초기 낙하 속도 y=4를 줘서 더 빨리 비워짐) 조준을 옆으로
  옮겼으면 즉시 다음 드롭. 같은 자리 연타 ~150ms, 좌우로 옮기며 연타하면 그보다 빠름.
  같은 자리에 겹쳐 스폰돼 튕기는 것만 막는 구조 (실측: 물리시간 기준 검증 완료)

## ✅ 2026-08-21: 운영 광고 ID + 인게임 UI 개편

앱인토스 **앱정보 승인**을 받아 운영 전환 작업을 했다.

- **광고 그룹 ID 운영(live) 전환** — `js/toss-ads.js`
  배너 `ait.v2.live.ac5c31a0cad14bcb`(문구강조형) / 보상형 `ait.v2.live.49083bc0d0d64743`.
  콘솔 MCP로 두 지면 모두 ENABLED 상태 확인함. AdMob(구글플레이용)은 여전히 테스트 ID.
- **인게임 HUD 대폭 정리** — 동전이 버튼에 가려 안 보이는 문제 + 우상단 토스 네이티브 ⋯/X 겹침 대응
  - 제거: 효과음·배경음악 음소거(→ 홈 화면 우상단 맨 위 `#menuSound` 가로 배치, 토스 안에서만
    네이티브 ⋯/X를 피해 64px로 내려감), 다시하기, 자동드롭 토글
  - 자동드롭은 **기본 켜짐**으로 고정 (`localStorage` 토글 삭제, 판 시작마다 안내 문구 1회)
  - 인게임 버튼은 **왼쪽 위 2×2** (❮ ⏩ / 🌀 🧹). 우상단은 토스 네이티브 버튼 자리라
    완전히 비움 (`#hudR` 삭제). 1줄 배치도 거쳤지만, 통을 낮춰 공간이 생긴 뒤 2×2로 최종 확정
  - **점수는 분홍 카드 없이 금액 텍스트만** (y≈78, 흰 외곽선 + 분홍/신기록 금색 글로우).
    카드 고정폭(216px)이 좁은 기기에서 버튼·네이티브 버튼과 겹치던 문제의 최종 해법
  - **통을 살짝 낮추고(LINE_Y 165→190) 낙하 대기 위치를 통에 붙여(DROP_Y 104→135)**
    상단 공간 확보. 점수는 이후 "맨 위 가운데(y=32)"로 최종 이동
- **지폐를 IconScout 실사 3D 일러스트로 교체** (Juty Jang 원화 지폐 시리즈 1000/5000/10000/50000)
  - 원본은 `brand/money/bill-*-raw.png`, 가공은 `node tools/money-assets.mjs`:
    기울어진 아트를 자동 탐색(트림 bbox 높이 최소각)으로 수평화 + 3.5% crop → `public/money/`
  - 지폐 물리 크기를 아트 비율(1.66)에 맞춰 변경(면적은 유지해 밸런스 동일), 그리기는 6% bleed
  - 처음엔 기울어진 아트 그대로 넣어 조각 사이 틈이 보였음 → 수평화로 해결
  - 동전은 IconScout에 액면별·스타일 맞는 게 없어 캔버스 그림 유지, 라벨의 "원"만 제거
  - 홈 화면 "돈 불리는 순서" 지폐 미리보기도 스프라이트로 표시
- **HUD 버튼 아이콘도 IconScout 3D로 교체** (이모지 ❮⏩🌀🧹 → PNG)
  - 뒤로가기(파랑 원+주황 ❮, Flat-Icons), 자동진행(보라 원 ▶▶, Creasheeps),
    통흔들기(청록 회오리, Rizal Bayu), 동전제거(민짜 금화(Supardi Lahadja)에 빨간 X를 tools/ui-icons.mjs가 가운데 합성 — 기성 Remove Coin은 X 배지가 구석이라 AD 배지에 가려져 탈락)
  - 원본 `brand/icons/*-raw.png`, 가공 `node tools/ui-icons.mjs` → `public/icons/` (96px, 개당 ~5KB)
  - 흰 회오리·연한 아이콘은 흰 버튼 위에서 안 보여 탈락시킴 — 진한 컬러 위주로 고른 것
  - 광고 팝업 타이틀·랭킹 뒤로가기 버튼도 같은 스프라이트로 통일 (이모지 잔재 정리:
    coin defs의 죽은 `sub` 필드, 캔버스 텍스트의 ⏩🌀, 탈락한 brand/money/coin-raw.png 삭제)
- **랭킹 점수 상한 추가** — `submit_score`가 1억 초과 점수를 조용히 무시 (조작 방어선).
  `db/ranking_v2.sql` 3) 블록 Supabase 적용 완료(8/21) — 2억 제출이 조용히 버려지는 것 서버에서 직접 검증함
- **소리 버그 2건 수정** — "음소거 해제가 안 먹힌다" 제보(실기기)로 발견
  - 배경음악을 **홈 화면에서도 재생**: 음소거 버튼을 홈으로 옮겼는데 재생 조건이
    `state === 'playing'`뿐이라 홈에서 해제해도 조용했다. goMenu/해제 핸들러/visibilitychange
    모두 menu 포함으로 확장, 첫 진입 시 playBgm + 자동재생 차단 대비 첫 터치 재시도
  - **광고 복귀 후 무음 회복**: 네이티브 전면 광고가 닫히면 AudioContext가 suspended로
    남아 "재생 중인데 무음"이 됨. resume은 제스처 안에서만 확실하므로 document
    pointerdown마다 ensureAudio + playBgm으로 회복 (효과음 blip도 컨텍스트 준비 전이면 150ms 지연)
  - **file://에서 배경음악만 무음이던 진짜 범인**: `createMediaElementSource`는 file://의
    <audio>를 cross-origin으로 취급해 스펙상 무음을 출력한다(에러 없음 → 효과음은 합성음이라
    정상, 배경음악만 죽음. localhost에선 정상이라 "됐다 안 됐다"로 보였다).
    file://일 땐 체인을 건너뛰고 volume 폴백으로 재생. 진단용 `__mm.audio()` 훅도 추가
  - 재확인 팝업은 홈(나가기) 전용으로 단순화 (다시하기 분기 삭제)
- **광고 팝업 개선** — 체감 로딩 속도 문제 대응
  - 팝업 타이틀을 "광고 보기" → 기능명("🌀 통 흔들기" / "🧹 동전 제거")으로
  - **팝업이 열리는 순간 광고 로드를 시작**하고, 로드가 끝나야 "광고 보고 사용하기" 버튼 활성화
    (`beginAdPrepare` + 브리지 `prepareRewarded` — toss/admob 두 브리지 동일 인터페이스)
  - 12초 안에 로드 실패/타임아웃 → "광고 다시 불러오기" 버튼 + 안내 문구로 전환, 재시도 가능
  - 팝업을 닫은 뒤 늦게 도착하는 로드 콜백은 시퀀스 번호(`adPrepareSeq`)로 무시
- **검증**: 로컬 서버 + 브라우저에서 HUD 구성·음소거 토글·자동드롭 연속 낙하·팝업
  로딩→활성화·실패→재시도·stale 콜백 무시·홈 재확인 팝업 전부 통과. `ait build` 성공.
- **광고 ID·Supabase 키는 .env로 안 뺀다**: 정적 클라이언트라 어차피 번들에 실려 공개되고,
  광고 ID는 앱에 묶여 도용 불가, Supabase anon key는 공개 전제 설계(RLS + security definer
  RPC만 허용, `db/ranking_v2.sql`). 진짜 비밀(키스토어, service_role)은 git에 없음을 확인.
- **최종 소스 점검 통과** (전 파일 정독): 브리지 3종 인터페이스 일치, 랭킹 닉네임 XSS 안전
  (전부 textContent), HTML id 참조 무결, 죽은 코드 없음. 유일한 수정: `ensureAudio`의
  resume() 거부가 unhandled rejection으로 쌓이던 것 catch 처리
- **안드로이드 업데이트 팝업 추가** — `js/app-update.js` + `#updateDlg`
  - webGame(src/shared/appUpdate.ts) 방식 이식: `@capawesome/capacitor-app-update`로
    Play에 직접 물어봄(버전을 어디에도 적지 않음), 자체 팝업, '업데이트' → 스토어 열림,
    '나중에' → 24시간 재질문 방지, 하드웨어 뒤로가기는 '나중에'로 처리
  - 토스·웹에서는 no-op. 새 플러그인이라 `cap sync android` 1회 실행함
- **versionCode 2 / versionName 1.1** — 오늘 변경분 전체 반영한 AAB 빌드 완료
  (`android/app/build/outputs/bundle/release/app-release.aab`, 7.8MB). 갤럭시(R3CW20FT8WV)에
  릴리스 빌드 설치·실행 확인 — 기존 Play 서명 설치본과 서명이 달라 제거 후 설치했음
  (다음에 Play 버전 설치하면 정상 복귀). AdMob은 여전히 테스트 ID(정식 출시 직전 교체 예정)

> 앱인토스 콘솔은 MCP(`apps-in-toss-console`)로 빌드~검수 요청까지 다룰 수 있다.
> workspaceId `61261`, miniAppId `65259`.
> **실제 출시(라이브 전환)는 MCP로 불가** — 콘솔 웹 '앱 출시' 화면에서 직접 눌러야 한다.
> 구글플레이는 MCP가 없어 AAB 빌드까지만 자동화되고 업로드는 사람이 한다.

---

## ✅ 완료한 것

### 1. Capacitor 안드로이드 포장 — `9bcf171`

앱인토스와 **같은 `dist/`** 를 그대로 APK/AAB 안에 넣는 구조. 웹 서버가 필요 없다.

| 파일 | 역할 |
|---|---|
| `capacitor.config.js` | `appId: com.moneygame.app`, `appName: 머니 게임`, `webDir: dist` |
| `android/` | `cap add android` 생성물. 화면 세로 고정 |
| `js/capacitor-bridge.js` | 하드웨어 뒤로가기 처리 |
| `js/legal-link.js` | 정책 링크를 환경별 인앱 브라우저로 열기 |
| `lib/capacitor.js` | `@capacitor/core` IIFE 번들 (직접 벤더링) |

작업 중 확인한 두 가지가 설계를 바꿨다.

- **`Capacitor.Plugins`는 자동으로 안 채워진다.** 네이티브 브리지가 주입하는 건 `isNativePlatform`
  같은 저수준 API뿐. 번들러가 없으니 `lib/capacitor.js`를 벤더링하고
  `capacitorExports.registerPlugin('App'|'Browser')`로 프록시를 직접 만든다.
- **토스 SDK에 `AppsInToss.openURL`이 있다.** 덕분에 정책 링크가 토스에서도 인앱 브라우저로 열린다.
  `target="_blank"`에 맡겼다면 웹뷰가 정책 페이지로 덮여 게임으로 못 돌아왔을 것.

**뒤로가기 동작 순서** — 위에 떠 있는 레이어부터:
닉네임 편집 → 광고 확인 → 재확인 팝업 → 랭킹 → 게임오버(처음으로) →
플레이 중(나가기 재확인) → 홈에서 두 번 누르면 종료. 광고 재생 중엔 무시.

### 2. 개인정보처리방침 — `9bcf171` + GitHub Pages 설정

- `docs/privacy.html` — 코드를 읽고 실제 수집 항목대로 작성
- Pages: `main` 브랜치 `/docs` 폴더 → **게임 본체는 웹에 안 올라감**
- 공개 주소: **https://tngus842655.github.io/MoneyGame/privacy.html**
- 게임 홈 화면 하단 링크(`#linkPrivacy`)가 이 주소와 일치하는 것 확인함

### 3. 게임물관리위원회 재신청 서류 — `4b87896`

`등급분류/` 폴더:

| 파일 | 용도 |
|---|---|
| `게임물개요.txt` | 신청 화면 개요 입력란에 붙여넣기 (1000자 제한, **872자**) |
| `게임물내용설명.txt` | 첨부용 내용설명서. 등급 기준 7개 요소별 서술 + 사행성 없음 근거 6가지 |
| `게임실행방법.txt` | 심사관용 실행 안내 |
| `README.md` | 제출용 zip 만드는 법 |

**등급분류 결정 완료 — 전체이용가, 등급분류번호 `SC-OM-260819-005` (2026-08-19).**
신청 등급대로 나왔다. 게임산업법상 표시 의무가 있어 `STORE_LISTING.md`의 자세한 설명
맨 아래에 넣어 두었다.

> 게임 내용을 바꾸면 **내용수정신고 대상인지 확인**해야 한다. 이 번호는 현재 내용
> (원화 단계, 한글 문구) 기준이다 — 아래 '나중에 — 다국어'와 직결된다.

**지난 반려 사유는 게임 문제가 아니었다.** 압축 해제 후 `index.html`을 브라우저로 여는 것만으로
정상 실행·플레이되는 것을 확인함 (`file://` 직접 열기, 데스크톱 1920×1080 / 1366×768).
실행 절차 안내가 없었던 것이 원인으로 보고, 서류 3종 모두에 실행 방법을 명시했다.

### 4. 패키지명 확정 — `0a26070`

`io.github.tngus842655.moneygame` → **`com.moneygame.app`**
appId가 바뀌면 자바 패키지 경로까지 바뀌므로 네이티브 프로젝트를 재생성했다.

### 5. 구글플레이 출시 준비

| 한 일 | 내용 |
|---|---|
| 런처 아이콘 | Capacitor 기본 로고 → 게임 아이콘. `tools/android-assets.mjs`가 원본 하나(`brand/`)에서 밀도 5종 + 적응형 + 스플래시 + 스토어 512를 생성 |
| 릴리스 서명 | `keystore.properties`(gitignore) 기반 `signingConfigs` + 키가 없으면 릴리스 빌드를 **일찍 실패**시키는 가드 |
| 빌드 단순화 | 웹 자산 동기화를 gradle에 물렸다. `android\`에서 `gradlew.bat clean` + `bundleRelease` **두 줄이면 끝** |
| R8 | `minifyEnabled true` — mapping.txt가 .aab에 실려 크래시 로그가 복원된다 |
| AdMob | `js/admob-ads.js` 신설. 배너 + 보상형, **구글 공식 테스트 ID** |
| 스토어 자산 | `store/icon-512.png`, `store/feature-graphic-1024x500.png` |
| 제출 문서 | `PLAY_CONSOLE.md`(데이터 안전·IARC), `STORE_LISTING.md`(문구), `ANDROID_RELEASE.md`(빌드) |

작업하며 확인한 것.

- **`file()`은 `android/app/` 기준으로 풀린다.** `storeFile`을 `file()`로 읽으면
  `android/`에 둔 키스토어를 못 찾는다. `rootProject.file()`로 바꿨다.
  webGame은 절대 경로를 써서 이 문제가 드러나지 않았다.
- **PowerShell 5.1은 BOM 없는 UTF-8 .ps1을 CP949로 읽는다.** 한글 주석의 마지막 바이트가
  줄바꿈까지 삼켜서 그 다음 줄이 주석에 묻혔다. `scripts/make-upload-key.ps1`은 BOM을 붙여 둔다.
- **`Set-Content -Encoding utf8`은 BOM을 쓴다.** 그렇게 만든 `keystore.properties`는
  java.util.Properties가 BOM을 첫 줄의 일부로 읽어 쓰레기 항목을 만든다. BOM 없이 쓰도록 했다.

- **광고 브리지를 `AdsBridge` 하나로 통일했다.** 게임(`game.js:597`)은 브리지 하나만 보고,
  토스·AdMob이 각자 자기 환경일 때만 그 자리를 차지한다. 토스 브리지가 환경과 무관하게
  자리를 잡고 있으면 AdMob이 영영 못 붙으므로, `toss-ads.js`는 실제로 광고를 띄울 수
  있을 때만 등록하도록 바꿨다.
- **네이티브 배너는 웹뷰 안이 아니라 그 위에 얹힌다.** body가 세로 가운데 정렬이라
  자리표시자가 화면 하단의 배너보다 60px쯤 위에 떠 있었다. `body.in-app`에
  `align-items:flex-end`를 줘서 아래로 붙였다. 하단 시스템 인셋은 플러그인이 알아서 띄워 준다.
- **적응형 아이콘 안전 영역이 제목을 잘라먹었다.** 아트워크 아래쪽 '머니게임' 글자가
  원형 마스크에서 좌우로 잘렸다. 68dp로 줄이고 3dp 위로 올려 원형·둥근사각 양쪽에서 살아난다.

### 6. 실기기 검증에서 잡은 버그 둘 — `d4b4656`, `aae7f6c`

Galaxy S23(Android 16)에 릴리스 빌드를 설치해 확인하다 나왔다. **둘 다 .aab만 봐서는
드러나지 않던 것들이다.**

**`cap sync`가 gradle 산출물을 지웠다.** `assembleRelease`/`installRelease`가
`processReleaseResources`에서 `capacitor-cordova-android-plugins/.../package-aware-r.txt`가
없다며 죽었다. `sync`는 `copy + update`인데 `update`가 그 모듈을 통째로 다시 만들면서
gradle이 방금 생성한 R 파일까지 지운다. 표식 파일을 심고 대조 실험으로 확인했다
(sync는 지우고 copy는 남긴다). 빌드마다 필요한 건 자산 복사뿐이라 `cap copy`로 바꿨다.
`bundleRelease`는 자원 처리 경로가 달라 우연히 살아남아 있었다.

**광고 후 남은 횟수 배지가 앱인토스에서 안 바뀌었다.** 기능은 실행되는데 배지가 3에
머물다가, 그 버튼을 한 번 더 누르면 2로 바뀌었다. 값은 이미 줄어 있었고, 배지를
`::before { content: attr(data-count) }`로 그린 게 원인이었다 — 토스는 광고가 웹뷰를
덮는 동안(`document.hidden`) 값이 바뀌므로 돌아와도 그 타일이 다시 그려지지 않았고,
한 번 더 누르면 `:active`의 transform이 재래스터해서 그제야 보였다.
배지를 실제 요소(`<span class="cnt">`)의 텍스트로 바꿔 없앴다.
**구글플레이(AdMob)에서는 재현되지 않았다** — 웹뷰 구현 차이로 보인다.

---

## 📋 남은 일

### 대기 중 — 검토 결과

- [ ] **앱인토스 앱정보 승인** (`IN_REVIEW`) → 승인되면 번들 `20260819-6` 검수 요청
- [ ] **구글플레이 검토** → 승인되면 비공개 테스트 트랙 운영
- [ ] 프로덕션 승격 전 **테스터 12명 × 14일** 비공개 테스트
      (14일은 12명이 **계속 옵트인 상태로 유지된** 기간이고, 테스터가 링크로
      옵트인을 완료해야 집계된다 — 설치만으로는 안 된다)

### 끝난 것

- [x] 업로드 키 생성 + 백업 — `android/moneygame-upload.keystore` (gitignore, 별도 백업 완료)
- [x] 스크린샷 — Play Console에 등록 완료
- [x] 실기기 확인 — Galaxy S23에서 배너 위치·뒤로가기·보상형 광고 지급까지 전부 통과
- [x] 등급분류번호 표기 — 전체이용가 / `SC-OM-260819-005`, `STORE_LISTING.md`에 반영
- [x] 게임 등급분류 토스 콘솔 등록

### 정식 출시로 승격하기 직전

- [ ] **AdMob 실제 ID로 교체** — `js/ads-config.js`의 `USE_TEST_ADS = false` + `REAL` 채우기,
      `AndroidManifest.xml`의 `APPLICATION_ID`도 함께. 테스트 ID인 채로 정식 출시하면 수익이 0원이다
- [ ] **토스 광고 ID 교체** — `js/toss-ads.js:12-13`이 아직 앱인토스 **테스트 ID**
      (`ait-ad-test-banner-id`, `ait-ad-test-rewarded-id`)

### 나중에 — 다국어

`js/game.js:25`의 `MODES.usd`가 이미 완성되어 있다 (1¢/5¢/10¢/25¢ + $1/$5/$10/$100).
남은 건 UI 문구 약 120개(index.html 30 / game.js 51 / ranking.js 41) 사전화와 언어 자동 선택.
`res/values-ko/strings.xml`을 추가하면 한국어 기기만 "머니 게임", 나머지는 영어 이름으로 뜬다.
**등급분류는 이미 받았다** (전체이용가 `SC-OM-260819-005`). 제출한 내용설명서는 현재
게임 내용(원화 단계, 한글 문구) 기준이라, 달러 모드나 영어 UI를 추가하면 게임 내용이 달라진다.
**착수 전에 내용수정신고 대상인지 확인할 것.** 스토어에 표기한 등급분류번호도 같이 영향받는다.

---

## 🔧 자주 쓸 명령어

```bash
# 앱인토스 배포 번들 — 빌드까지. 올리는 건 아래 참고
npm run build:toss          # dist/ 준비 + moneygame.ait 생성 (deploymentId를 찍어 준다)
# npm run deploy:toss 는 배포 API 키를 물어본다. 키 없이 올리려면 MCP를 쓴다:
#   bundle_upload → uploadUrl에 .ait를 curl -X PUT → bundle_upload_complete
#   → bundle_build_status로 대기 → bundle_test_push → bundle_submit_review

# 안드로이드 릴리스 — versionCode 올리고 cmd에서 android\ 들어가 두 줄이면 끝.
# 웹 자산 동기화는 gradle이 알아서 한다 (makeWebDist → capCopy → preBuild).
gradlew.bat clean
gradlew.bat bundleRelease
gradlew.bat signingReport   # 서명이 붙었는지 (Config가 null이 아니면 정상)

# 처음 한 번만 — 업로드 키. 반드시 터미널 창에서 (비밀번호를 물어봄)
powershell -ExecutionPolicy Bypass -File scripts/make-upload-key.ps1

# 아이콘·스토어 자산 다시 만들기 (brand/의 원본을 갈아 끼운 뒤)
npm run icons

# 로컬 테스트
npx http-server -p 5317 -c-1 .

# GMC 제출용 zip (압축 풀면 index.html 옆에 실행방법.txt가 보이게)
node scripts/make-dist.mjs
cp 등급분류/게임실행방법.txt dist/실행방법.txt
(cd dist && zip -r ../dist.zip .)
```

---

## ⚠️ 까먹기 쉬운 것

1. **웹 자산 동기화는 이제 gradle이 알아서 한다** (`makeWebDist` → `capCopy` → `preBuild`).
   예전처럼 `npm run build:android`를 먼저 칠 필요가 없다.
   단, **플러그인을 더하거나 뺐을 때는** `npm run build:android`(전체 `cap sync`)를
   한 번 돌려야 네이티브 쪽에 반영된다 — 빌드에 물린 건 `cap copy`(자산 복사)뿐이다.
   `android/app/src/main/assets/public`은 복사본이라 커밋되지 않는다.
2. **`rm -rf android`로 재생성하면 화면 세로 고정이 날아간다.**
   `android/app/src/main/AndroidManifest.xml`의 activity에 `android:screenOrientation="portrait"`를
   다시 넣어야 한다. `cap add android` 기본 매니페스트에는 없는 수동 설정이다.
3. **패키지명 `com.moneygame.app`은 플레이스토어 첫 업로드 후 영구히 못 바꾼다.**
4. **서명 키(`*.jks`)를 잃어버리면 앱 업데이트가 영구히 불가능하다.** `.gitignore`에 있으니 따로 백업할 것.
5. 정책 URL을 바꾸면 `index.html`의 `#linkPrivacy` 링크도 같이 고쳐야 한다.
6. `lib/capacitor.js`는 수동 벤더링이다. `@capacitor/core` 버전을 올리면
   `cp node_modules/@capacitor/core/dist/capacitor.js lib/`로 직접 갱신해야 한다.
7. 앱인토스 쪽 `appName: 'moneygame'`(`apps-in-toss.config.ts`)은 Capacitor의 `appName`과 **다른 값**이다.
   토스 콘솔 등록명과 일치해야 한다.
8. **AdMob ID는 두 군데다.** 광고 단위는 `js/ads-config.js`(웹), 앱 ID는
   `AndroidManifest.xml`(네이티브). 하나만 바꾸면 광고가 안 나오거나 앱이 죽는다.
9. **아이콘 원본은 `brand/`에 둔다.** `public/`에 두면 `make-dist.mjs`가 `dist/`로 복사해
   토스 번들과 APK에 650KB가 그대로 실린다.
10. `android/local.properties`(SDK 경로)도 커밋되지 않는다. 다른 PC에서 빌드하려면
    `sdk.dir=...`을 새로 적어야 한다.

---

## 🧪 검증한 내용

| 항목 | 결과 |
|---|---|
| 일반 브라우저에서 기존 동작 회귀 | 두 브리지 모두 no-op, 게임 정상, 페이지 에러 0 |
| 안드로이드 네이티브 경로 (플러그인 스텁) | 뒤로가기·종료·정책 링크 **15개 항목 전부 통과** |
| `file://` 직접 열기 | 정상 실행·플레이 (심사관 시나리오) |
| 데스크톱 1920×1080 / 1366×768 | 정상 실행·플레이 |
| 압축 해제 → index.html 더블클릭 | 정상 (돈 합쳐져 점수 100점까지 확인) |

### 실기기 (Galaxy S23 / Android 16 / 1080×2340 / 480dpi)

릴리스 빌드(R8 + 실제 서명)를 `gradlew installRelease`로 설치해 확인했다.

| 항목 | 결과 |
|---|---|
| 설치·실행 | 통과 |
| AdMob 배너 위치 | 하단 정위치, 어두운 자리표시자 안 보임. 자동 갱신도 정상 |
| 게임 플레이·합치기 | 통과 |
| 뒤로가기 (플레이 중) | 재확인 팝업, 앱 안 꺼짐 |
| 보상형 광고 — 통 흔들기 | 지급 + 3→2 차감 |
| 보상형 광고 — 동전 제거 | 3→2→1→0 차감, 0에서 버튼 비활성화 |
| 웹뷰 콘솔 에러 | 0개 |

앱인토스는 테스트 푸시(`20260819-6`)로 확인 — 배지 갱신 문제가 이 번들에서 해결된 것을
사용자가 직접 확인했다.

> **로컬 설치본과 Play 배포본은 서명이 다르다.** 로컬은 업로드 키, Play는 구글의 앱 서명 키로
> 다시 서명된다. Play에서 설치하려면 로컬 설치본을 **먼저 지워야** 한다
> (`adb uninstall com.moneygame.app`). 검증 후 지워 뒀다.

**알려진 제약**: PC에서 `file://`로 열면 브라우저 보안 정책상 배경음악이 무음이다.
효과음과 게임 진행은 정상이고 실제 배포본에서는 소리가 난다. GMC 서류에 미리 적어 뒀다.
