# Play Console 제출 체크리스트

머니 게임(`com.moneygame.app`)을 구글플레이에 올릴 때 콘솔에서 채워야 하는 것들.
빌드·서명 절차는 `ANDROID_RELEASE.md`, 스토어 문구는 `STORE_LISTING.md`에 있다.

| 항목 | 값 |
| --- | --- |
| 패키지 이름 | `com.moneygame.app` (첫 업로드 후 **영구히 못 바꿈**) |
| 최초 버전 | versionCode `1` / versionName `1.0` |
| 개인정보처리방침 | https://tngus842655.github.io/MoneyGame/privacy.html |
| 문의 이메일 | tngus842655@gmail.com |
| 광고 | 있음 (AdMob) |
| 인앱 구매 | 없음 |

---

## 1. 앱 콘텐츠 — 데이터 안전 섹션

개인정보처리방침(`docs/privacy.html`)에 적은 내용과 **한 글자도 어긋나면 안 된다.**
어긋나면 반려되고, 나중에 발견돼도 앱이 내려간다.

**공통 답변**

- 전송 중 데이터 암호화: **예** (모든 통신 HTTPS)
- 데이터 삭제 요청 방법 제공: **예** → 개인정보처리방침 URL 기재
- 아동 대상 앱: **아니요** (만 14세 미만 대상 아님)

**수집·공유 항목**

| Play 분류 | 실제 데이터 | 수집 | 공유 | 목적 | 필수 여부 |
| --- | --- | --- | --- | --- | --- |
| 개인정보 › 사용자 ID | 랭킹 닉네임(최대 10자), 기기에서 만든 무작위 식별자 | 예 | 아니요 | 앱 기능 | 선택 (랭킹 안 쓰면 전송 없음) |
| 앱 활동 › 앱 상호작용 | 점수·기록 시각 | 예 | 아니요 | 앱 기능 | 선택 |
| 기기 또는 기타 ID | 광고 ID | 예 | **예** (Google AdMob) | 광고, 사기 방지 | 필수 |

> 광고 ID를 '공유'로 표시해야 하는 이유: AdMob SDK가 구글로 보낸다.
> `AndroidManifest.xml`의 `com.google.android.gms.permission.AD_ID` 권한이 그 근거다.

**수집하지 않는 것** (질문에 나오면 전부 '아니요'):
이름, 이메일, 전화번호, 주소, 결제·금융 정보, 위치, 주소록, 사진·파일, 통화 기록,
소셜 로그인, 기기 고유번호(IMEI 등)

---

## 2. 앱 콘텐츠 — 나머지 신고

| 질문 | 답 | 비고 |
| --- | --- | --- |
| 광고 포함 | **예** | AdMob 배너 + 보상형 |
| 콘텐츠 등급 설문(IARC) | 작성 필요 | 아래 참고 |
| 타겟층 | 만 13세 이상 | 아동(만 13세 미만) 대상 아님 |
| 뉴스 앱 | 아니요 | |
| 코로나19 접촉자 추적 | 아니요 | |
| 데이터 보안 | 위 1번 | |
| 정부 앱 | 아니요 | |
| 금융 기능 | **아니요** | 게임 속 '돈'은 점수일 뿐 실제 금전 거래가 없다 |

**IARC 설문에서 주의할 것**

- 폭력·성적 내용·비속어·약물: 전부 없음
- 도박/사행성: **없음.** 확률형 아이템도, 실제 돈이 오가는 요소도 없다
  (근거는 `등급분류/게임물내용설명.txt`에 정리해 둔 6가지)
- 이용자 간 상호작용: **닉네임이 랭킹에 공개된다**는 점은 있는 그대로 신고할 것.
  채팅·메시지·사진 공유는 없다
- 광고 표시: 예

**이미 받아 둔 국내 등급분류**

| 항목 | 값 |
| --- | --- |
| 등급 | 전체이용가 |
| 등급분류번호 | `SC-OM-260819-005` |
| 결정일 | 2026-08-19 |

> 구글플레이의 콘텐츠 등급은 IARC 설문으로 매겨지고, 한국 등급(GRAC)도 여기서 같이 나온다.
> 게임물관리위원회에서 따로 받은 위 등급분류는 앱인토스 등 다른 유통 경로용이다.
> **IARC 설문 결과가 전체이용가로 나오는지 확인할 것** — 국내 등급과 어긋나면
> 설문 답변을 잘못 고른 것이다.
>
> 등급분류번호는 게임산업법상 표시 의무가 있어 스토어 설명에 넣어 두었다
> (`STORE_LISTING.md`).

---

## 3. 스토어 등록정보에 올릴 자산

`store/` 폴더에 있다.

| 자산 | 규격 | 파일 | 상태 |
| --- | --- | --- | --- |
| 앱 아이콘 | 512×512 32비트 PNG | `store/icon-512.png` | ✅ |
| 피처 그래픽 | 1024×500 알파 없는 PNG/JPEG | `store/feature-graphic-1024x500.png` | ✅ |
| 휴대전화 스크린샷 | 2~8장, 16:9 또는 9:16, 한 변 320~3840px | `store/screenshots/` | ⬜ 실기기에서 촬영 |
| 7인치 태블릿 | 선택 | — | 세로 고정 게임이라 생략 |

> 스크린샷은 실기기에서 찍는 게 가장 정확하다. 폰에서 게임을 켜고 전원+음량↓로 촬영해
> `store/screenshots/`에 넣으면 된다. 메뉴 화면 / 플레이 중 / 랭킹 / 게임오버 4장이면 충분하다.

---

## 4. 출시 전에 반드시 확인

- [ ] **업로드 키 생성·백업** — `scripts/make-upload-key.ps1`.
      잃어버리면 이 패키지는 영영 업데이트할 수 없다
- [ ] **Play 앱 서명 사용** — 콘솔이 배포용 키를 대신 보관해 준다. 첫 업로드 때 켠다
- [ ] **AdMob 실제 ID로 교체** (정식 출시 승격 직전)
      - `js/ads-config.js`의 `USE_TEST_ADS = false` + `REAL` 채우기
      - `android/app/src/main/AndroidManifest.xml`의 `APPLICATION_ID`를 실제 앱 ID로
      - 바꾼 뒤 `gradlew.bat bundleRelease`만 다시 돌리면 된다 (웹 자산은 gradle이 알아서 갱신)
- [ ] **비공개 테스트 12명 × 14일** — 2023년 11월 이후 만든 개인 개발자 계정이면
      프로덕션 승격 전에 필수다. 기존에 앱을 낸 계정이면 이미 지났을 수 있으니
      콘솔의 안내 배너로 확인할 것
- [ ] **실기기 확인** — 뒤로가기 동작, 정책 링크, 배너 위치, 보상형 광고 지급

---

## 5. 광고 ID 교체 후 주의

실제 광고 단위 ID로 바꾼 뒤에는 **본인 기기에서 광고를 클릭하지 말 것.**
무효 트래픽으로 판정되면 AdMob 계정이 정지되고 수익이 회수된다.
동작 확인은 `USE_TEST_ADS = true`로 되돌려서 한다.

---

## 6. 자동으로 붙는 권한 (놀라지 말 것)

AdMob SDK(`play-services-ads`)가 매니페스트 병합으로 끌고 오는 것들이다.
우리 코드가 요청한 게 아니고, 빼면 광고가 동작하지 않는다.

| 권한 | 출처 |
| --- | --- |
| `ACCESS_NETWORK_STATE` | 광고 요청 전 연결 상태 확인 |
| `com.google.android.gms.permission.AD_ID` | 광고 ID (데이터 안전에 신고함) |
| `ACCESS_ADSERVICES_AD_ID` / `_ATTRIBUTION` / `_TOPICS` | 안드로이드 프라이버시 샌드박스 |
| `WAKE_LOCK`, `FOREGROUND_SERVICE` | AdMob이 쓰는 WorkManager(androidx.work)가 선언 |

`FOREGROUND_SERVICE`가 붙어 있어 콘솔이 '포그라운드 서비스' 신고를 요구할 수 있다.
우리 앱은 포그라운드 서비스를 **직접 시작하지 않고**, 병합된 서비스에도
`foregroundServiceType`이 없다. 질문이 뜨면 그대로 설명하면 된다.

확인 방법:

```bash
java -jar C:/Workspace/bundletool-all-1.18.3.jar dump manifest --bundle=android/app/build/outputs/bundle/release/app-release.aab
```
