{
  "name": "money-game",
  "private": true,
  "scripts": {
    "build": "node scripts/make-dist.mjs && ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.0.3"
  },
  "devDependencies": {
    "@apps-in-toss/devtools": "^3.0.3"
  }
}
